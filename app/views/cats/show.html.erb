<center>

<img src= "<%= @cat.img_url %>">

<p><h1><%= @cat.name %></h1></p>
<p>Gender: <%= @cat.gender %></p>
<p>Age: <%= @cat.age %></p>
<p>Owners: 
<% @owners.each do |owner| %>
    <li>
    <%= owner.name %>
    <% @adoptions.each do |adopt| %>
        <% if adopt.owner == owner %>
            <%= button_to 'adoption information', adoption_path(adopt), method: :get%>
        <% end %>
    <% end %>
    </li>
<% end %>
</p>